module.exports = {
    siteUrl: 'https://example.com', // replace with your site URL
    outputFolder: 'build', // replace with the name of your build folder
    ignoredPaths: ['/admin/*'], // replace with any paths you want to exclude from the sitemap
  };
  